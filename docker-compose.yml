version: '3'
services:
    vpn:
        networks:
            wg-vpn:
        build: .
        volumes:
            - pia:/pia
        privileged: true
        cap_add:
            - NET_ADMIN
            - SYS_MODULE
        environment:
            #- LOCAL_NETWORK=192.168.69.0/24
            #- ALLOW_DOCKER=1
            - LOC=swiss
            - USER=p0000000
            - PASS=xxxxxxxxxxxxxxxx

volumes:
    pia:
    
networks:
    wg-vpn: